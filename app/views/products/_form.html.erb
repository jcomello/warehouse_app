<%= simple_form_for @product do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :provider %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, value: t('buttons.save') %>
  </div>
<% end %>
